import {prisma} from "@/lib/prisma";export default async function Page(){const [p,c,a]=await Promise.all([prisma.product.count(),prisma.category.count(),prisma.product.count({where:{available:true}})]);return <main className="container py-10"><h1 className="text-3xl">Cardapio</h1><div className="grid grid-cols-2 gap-4 py-8">{[["Produtos",p],["Categorias",c],["Disponiveis",a],["Esgotados",p-a]].map(x=><article className="bg-[#130708] p-5" key={x[0]}><b>{x[0]}</b><p className="text-3xl">{x[1]}</p></article>)}</div></main>}
