import {PrismaClient} from "@prisma/client";import bcrypt from "bcryptjs";const p=new PrismaClient();
async function main(){const email=process.env.ADMIN_EMAIL!,password=process.env.ADMIN_PASSWORD!;if(!email||password.length<10)throw Error("Configure admin env");await p.admin.upsert({where:{email},update:{passwordHash:await bcrypt.hash(password,12)},create:{email,passwordHash:await bcrypt.hash(password,12)}});await p.restaurantSettings.upsert({where:{id:"default"},update:{},create:{}});await p.category.upsert({where:{slug:"geral"},update:{active:true},create:{name:"Geral",slug:"geral",active:true}})}main().finally(()=>p.$disconnect());
